<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
      margin: 0;
      font-family: Arial, sans-serif;
      background: #f5f5f5;
    }
    .content {
      margin-left: 200px;
      padding: 40px;
      display: flex;
      gap: 20px;
      margin-top: -360px;; 
    }
    .box {
      background-color: white;
      border-radius: 10px;
      padding: 20px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
      flex: 1;
    }
    .admin-action {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 15px;
    }
    .admin-action form {
    margin: 0;
    }
    
    </style>
</head>
<body>
     <%- include("dashboard.ejs") %>
     <div class="content">
    <div class="box">
    <h2>Welcome to Exam Application System</h2>
    <table>
    <thead>
      <tr>
        <th>ID</th>
        <th>Name</th> 
        <th>Password</th>
      </tr>
    </thead>
    <tbody>
         <% admin.forEach(admin => { %>
        <tr>
          <td><%= admin.adm_id %></td>
          <td class="admin-action">
            <span><%= admin.aname %></span><th>
            <span><%= admin.apassword %></span>
            <form action="adminadd" method="GET" style="display: inline;">
              <input type="hidden" name="adm_id" value="<%= admin.adm_id %>"><th>
              <input type="hidden" name="aname" value="<%= admin.aname %>"><th>
              <input type="hidden" name="apassword" value="<%= admin.apassword %>"><th>
            </form>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</div>
</div>
</body>
</html>